#pragma once
#include<iostream>
#include <string>
using namespace std;

class DBManager
{
public:
	DBManager();
	~DBManager();

	bool Login(const string& ID, const string& password)
	bool Register(Member);
	// Member 는 PC managerment 클라이언트에서 받아 옵니다.(?)

private:

};
